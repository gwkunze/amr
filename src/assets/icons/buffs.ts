import surgeProtectionIcon from '../../../img/buff/surge-protection.png';

import { ImageSet } from './types';

export const Buffs: ImageSet = new Map([
    ['Surge Protection', surgeProtectionIcon],
]);

